# data/legacy_ambiguous_fingers

Aqui se guardan CSV historicos con etiquetas como `dedo_pulgar` o
`dedo_indice`.

Esas etiquetas no indican si el dedo se estaba abriendo o cerrando, por eso no
se usan para entrenar el modelo direccional actual.
